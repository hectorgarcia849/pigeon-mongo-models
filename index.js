const {User} = require('./models/user');
const {Pigeon} = require('./models/pigeon');
const {Profile} = require('./models/profile');
const {Chat, Messages, Message, Chats} = require('./models/chat');

module.exports = {User, Pigeon, Profile, Chat, Chats, Messages, Message};